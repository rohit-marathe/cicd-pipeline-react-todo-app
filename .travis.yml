sudo: required
services:
    - docker
before_install:
  - docker build -t myreact-cicd .

script:
  - echo "everything is working"

#Build our deployment container
before_deploy:
  - docker build -t myreact-cicd .

deploy:
  skip_cleanup: true
  provider: script
  on:
    branch: main
